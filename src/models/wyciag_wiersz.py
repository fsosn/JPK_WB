class WyciagWiersz:
    def __init__(
        self,
        numer_wiersza,
        data_operacji,
        nazwa_podmiotu,
        opis_operacji,
        kwota_operacji,
        saldo_operacji,
    ):
        self.numer_wiersza = numer_wiersza
        self.data_operacji = data_operacji
        self.nazwa_podmiotu = nazwa_podmiotu
        self.opis_operacji = opis_operacji
        self.kwota_operacji = kwota_operacji
        self.saldo_operacji = saldo_operacji
